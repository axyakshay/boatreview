<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-md-4">
        <%= image_tag @boat.image.url(:medium) %>
        <div class="star-rating" data-score= <%= @avg_review %> ></div>
        <em><%= "#{@reviews.length} reviews" %></em>
        <div class="table-responsive">
          <table class="table">
            <tbody>
              <tr>
                <td><strong>Title:</strong></td>
                <td><%= @boat.title %></td>
              </tr>
              <tr>
                <td><strong>Description:</strong></td>
                <td><%= @boat.description %></td>
              </tr>
              <tr>
                <td><strong>boat Dimensions:</strong></td>
                <td><%= @boat.boat_dimensions %></td>
              </tr>
              <tr>
                <td><strong> Place:</strong></td>
                <td><%= @boat.place %></td>
              </tr>
              <tr>
                <td><strong>Rating:</strong></td>
                <td><%= @boat.rating %></td>
              </tr>
            </tbody>
          </table>
          <%= link_to "Write a Review", new_boat_review_path(@boat) %>
        </div>
      </div>
      <div class="col-md-7 col-md-offset-1">
        <h1 class="review_title"><%= @boat.title %></h1>
        <p><%= @boat.description %></p>

        <% if @reviews.blank? %>
          <h3>No reviews just yet, would you like to add the first!</h3>
          <%= link_to "Write Review", new_boat_review_path(@boat), class: "btn btn-danger" %>
        <% else %>
          <% @reviews.each do |review| %>
            <div class="reviews">
              <div class="star-rating" data-score= <%= review.rating %> ></div>
              <p><%= review.comment %></p>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= link_to 'Edit', edit_boat_path(@boat) %> |
<%= link_to 'Back', boats_path %>

<script>
    $('.star-rating').raty({
      path: '/assets/',
      readOnly: true,
      score: function() {
            return $(this).attr('data-score');
    }
  });
</script>
